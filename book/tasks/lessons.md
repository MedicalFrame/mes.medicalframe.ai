# MESbook 작업 Lessons

## 2026-06-05

- 관리 문서의 상태 판단보다 `book_manuscript/` 실파일 존재 여부를 먼저 확인한다.
- “파일 없음” 판단은 실제 트리 재스캔 후에만 적는다.
- 짧은 장은 누락으로 오인하기 쉽지만, 브리지 장이나 선언문일 수 있으므로 본문 확인 후 분류한다.
- 프로젝트 운영 지침이 `tasks/` 기준이라면, 루트 `todo.md`만 두지 말고 `tasks/todo.md`에도 현재 상태를 남긴다.

## 2026-06-06

- 상태표와 매핑표는 과거 원본 폴더 기준이 아니라 `book_manuscript/`와 `00_management/scripts/build_book.py` 기준으로 유지해야 드리프트가 줄어든다.
- 빌드 참조 파일인 `user/의공모.docx`가 빠지면 `build_all.sh`가 바로 깨지므로, 삭제 여부를 먼저 확인한다.
- 마감 단계에서는 “원고 부족”보다 “문서/스크립트/산출물의 기준 불일치”가 더 큰 리스크가 된다.

## 2026-07-05

- 레포를 조직으로 이전한 뒤에는 원고 본문, handoff 산출물, QA 리포트에 남은 GitHub URL도 함께 점검해야 링크 기준이 어긋나지 않는다.
- 원본 소스를 사이트 레포의 `book/` 아래로 흡수하면 공개 파일과 원본 산출물의 해시를 비교해 같은 파일인지 확인한다.
